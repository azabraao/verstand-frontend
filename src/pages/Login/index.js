import React, { memo } from "react";
import LoginStyle from "./styles";

const Login = () => {
  return (
    <LoginStyle>
      <h1>Verstans</h1>
    </LoginStyle>
  );
};

export default memo(Login);
